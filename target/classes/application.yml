server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5342/bank_db
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath*:db/migration/db.changelog-master.yaml

  jwt:
    secret: "myamyamyamya123!@#supersecretkey2025"
    expiration: 36000000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
